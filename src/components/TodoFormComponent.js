/**
 * TodoFormComponent - Componente de formulario de tarea
 * (Actualmente integrado en TodoListComponent)
 */
export class TodoFormComponent {
  constructor(appManager) {
    this.appManager = appManager;
  }

  render(element) {
    // Implementación aquí
  }
}
