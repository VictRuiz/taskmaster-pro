/**
 * ProjectFormComponent - Componente de formulario de proyecto
 * (Actualmente integrado en HeaderComponent)
 */
export class ProjectFormComponent {
  constructor(appManager) {
    this.appManager = appManager;
  }

  render(element) {
    // Implementación aquí
  }
}
